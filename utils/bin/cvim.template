#!/bin/sh

export CANDYVIM_RUNTIME_DIR="${CANDYVIM_RUNTIME_DIR:-RUNTIME_DIR_VAR}"
export CANDYVIM_CONFIG_DIR="${CANDYVIM_CONFIG_DIR:-CONFIG_DIR_VAR}"
export CANDYVIM_CACHE_DIR="${CANDYVIM_CACHE_DIR:-CACHE_DIR_VAR}"

exec nvim -u "$CANDYVIM_RUNTIME_DIR/cvim/init.lua" "$@"
